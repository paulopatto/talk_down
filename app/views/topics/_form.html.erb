<%= simple_form_for topic, html: { class: "form-vertical col-md-6 center-block" } do |f| %>
  <%= f.input :title, class: 'form-group' %>
  <%= f.input :body, as: :text %>

  <%= f.submit "Criar Tópico", class: 'btn btn-default' %>
<% end %>
