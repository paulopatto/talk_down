<%= simple_form_for(@uglyterm) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :term %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
