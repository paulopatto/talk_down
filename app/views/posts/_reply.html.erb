<%= simple_form_for Post.new, url: reply_post_path(post_id),  html: { class: "form-horizontal" } do |f| %>
  <%= hidden_field_tag 'topic_id', topic_id %>
  <%= f.input :body %>
  <%= f.submit %>
<% end %>
